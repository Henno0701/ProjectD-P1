import { Button, Text, View } from 'react-native';

export default function StationsScreen() {
    return (
      <View className="flex-1 justify-center bg-main_bg_color items-center">
        <Text className="text-xl text-[#fff]">Stations Screen</Text>
      </View>
    );
  }